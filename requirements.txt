# Exact-pinned to the versions verified end-to-end on the dev machine
# (D-7.3): what was tested is what deploys. Direct dependencies only —
# transitive deps (torch, transformers, langchain-core) are left for pip
# to resolve so the solver is not over-constrained.
streamlit==1.57.0
langchain==1.2.18
langchain-openai==1.2.1
langchain-qdrant==1.1.0
langchain-community==0.4.1
langgraph==1.1.10
qdrant-client==1.17.1
openai==2.36.0
sentence-transformers==5.4.1
langfuse==4.6.1
python-dotenv==1.2.2
pandas==3.0.2
matplotlib==3.10.9
